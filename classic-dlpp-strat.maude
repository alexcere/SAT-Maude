sload classic-dlpp

smod CLASSIC-DLPP-STRATEGY is protecting CLASSIC-DLPP .

     strats basic-strat basic-iteration success-branch :   @ BasicSequent .

     sd basic-iteration := UnitPropagate ! ; Backtrack ? basic-iteration : (Fail or-else success-branch) .

     sd success-branch := one(Decide) ? basic-iteration : idle .

     sd basic-strat := PureLiteral ! ; basic-iteration .
     
endsm